# Баримт бичиг · Орчуулга (Word / Excel / PDF → 日本語 / English)

Байгууллагад зориулсан веб. Word, Excel, PDF, зурган draft-аа оруулж, засварлаж,
**Япон** болон **Англи** хэл рүү орчуулаад, **PDF** эсвэл **Word** болгож гаргана.

## Боломжууд
- **Оруулах (import):** `.docx` (зурагтай), `.xlsx/.xls` (хүснэгт + зураг), `.pdf` (текст), зураг (`.png/.jpg…`)
- **Засах:** тод/налуу, гарчиг, жагсаалт, зэрэгцүүлэлт, зураг чирж оруулах — веб дээрх засварлагч
- **Албан толгой:** байгууллага/огноо/дугаар/гарын үсэг бүхий бэлэн загвар нэг товчоор
- **Орчуулах:** Эх ↔ 日本語 ↔ English гурвыг хажуу хажууд нь; "→ 2 хэл" нэг товчоор хоёуланг нь
- **Автомат хадгалалт:** засвар бүр браузерт хадгалагдана, дахин нээхэд draft сэргэнэ
- **Гаргах:** Word (.docx) татах, **PDF — LaTeX-ээр албан бичгийн стандарт хэлбэрээр** (A4, MNS захын зай, хэлээр нь фонт, хуудасны дугаар)

## Ажиллуулах
```bash
npm install
cp .env.example .env      # тохиргоо
npm start                 # → http://localhost:3000
```

## Орчуулгын провайдер тохируулах
`.env` файлын `TRANSLATE_PROVIDER`-ийг сонгоно:

| Утга | Тайлбар | Хэрэгтэй зүйл |
|------|---------|----------------|
| `demo` | API key шаардахгүй, туршихад (жинхэнэ орчуулга хийхгүй) | — |
| `claude` | Албан бичгийн өнгө аяс, форматыг хамгийн сайн хадгална | `ANTHROPIC_API_KEY` |
| `deepl` | Япон–Англи орчуулгад маш сайн | `DEEPL_API_KEY` |
| `google` | Хямд, олон хэл | `GOOGLE_TRANSLATE_API_KEY` |

Гурвуулаа HTML-ийн бүтэц (зураг, формат) хэвээр хадгалж орчуулна.
Провайдер солиход код өөрчлөх шаардлагагүй — зөвхөн `.env`.

## Бүтэц
- `server.js` — Express сервер, API замууд
- `lib/parse.js` — файл → HTML (docx/xlsx/pdf/зураг)
- `lib/translate.js` — залгаж/сольж болдог орчуулга (claude/deepl/google/demo)
- `lib/export.js` — HTML → Word (.docx)
- `public/` — веб интерфейс (засварлагч, 3 багана, гаргац)

## Тэмдэглэл
- **Скан хийсэн (зураг) PDF** — текст агуулаагүй бол OCR шаардлагатай (одоогоор ороогүй; хэрэгтэй бол `tesseract.js`-ээр нэмж болно).
- **PDF гаргац** нь `tectonic` (LaTeX/XeTeX)-ээр серверт үүснэ. Албан бичгийн стандарт: A4, захын зай (зүүн 30мм · баруун 15мм · дээд/доод 20мм), 1.5 мөр хоорондын зай, хуудасны дугаар. Фонт: Япон бол Hiragino Mincho (明朝), бусад бол Times New Roman (Кирилл+Латин).
- **Шаардлага:** PDF гаргахад `tectonic` суусан байх ёстой (`~/.local/bin/tectonic`). Анх удаа компайл хийхэд LaTeX багцуудыг татдаг тул интернет хэрэгтэй, дараа нь кэшлэгдэж хурдан болно.
- **Excel-ийн зураг** — гарч ирнэ, гэхдээ анхны байрлал биш, хүснэгтийн доор нэмэгдэнэ (шаардлагатай газарт нь чирж зөөнө).
